extends layout.pug

block content 
  nav.grid-container.portfolio-breadcrumb
    a(href='/') 
     i(class="fas fa-arrow-circle-left")  Back
  article.grid-container.portfolio-intro.portfolio-project
    .grid-x
      .cell.small-6.medium-9.large-9
        //- Title
        h1= project.project_name
        //- Description
        p.lead.text-light= project.description

      .cell.portfolio-meta.small-6.medium-2.large-3
        h6 Technologies
        //- Looping over the project array to display the technologies that was used for this project
        ul
        each tech in project.technologies
          li= tech
        //- Links for the live preview and the repo
        a.button.expanded(href=`${project.live_link}`, target="_blank") Live Demo
        a.button.expanded(href=`${project.github_link}`, target="_blank") GitHub Repo
    
    //- Project images
    each image in project.image_urls
     img.image(src=`${image}`)